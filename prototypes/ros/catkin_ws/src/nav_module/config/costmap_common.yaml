# Common settings for both global and local costmaps
obstacle_range: 2.5
raytrace_range: 3.0
robot_radius: 0.2
inflation_radius: 0.5

observation_sources: scan
scan: {sensor_frame: laser, data_type: LaserScan, topic: /scan, marking: true, clearing: true}
